# 04 超导发展知识图谱：后端优化调整方案

## 核心目标

把当前 RAG 内部使用的 KG 查询能力抽象为独立服务，为未来图谱页面和 RAG 共同复用。

## 规划内容

- 复用 `papers`、`superconductors` 和 `superconductor_records` 作为事实来源。
- 暂不引入独立图数据库，优先用关系型查询生成局部图。
- 后续可增加关系缓存表，提高高频查询性能。

## 验收标准

- KG 查询结果与主数据库一致。
- 审核状态过滤正确。
- 查询服务可被 RAG 和前端图谱复用。
