# ============================================================
# SC-Wiki 依赖清单
# 安装: pip install -r requirements.txt
# ============================================================

# ── Web 框架 ────────────────────────────────────────────────
fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.6
jinja2>=3.1

# ── 数据库 ──────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0
alembic>=1.13
pymysql>=1.1          # MySQL (生产 sync)
asyncmy>=0.2           # MySQL (异步，RAG)
aiosqlite>=0.20        # SQLite (开发)

# ── 向量数据库 ──────────────────────────────────────────────
qdrant-client>=1.12

# ── LLM / Embedding ─────────────────────────────────────────
openai>=1.0            # DeepSeek + OpenAI 兼容

# ── 数据校验 ────────────────────────────────────────────────
pydantic>=2.5
pydantic-settings>=2.1
email-validator>=2.3

# ── 认证与安全 ──────────────────────────────────────────────
bcrypt==4.0.1          # 固定版本，5.x 与 passlib 不兼容
passlib[bcrypt]>=1.7
python-jose[cryptography]>=3.3

# ── HTTP / 异步 ─────────────────────────────────────────────
httpx>=0.26
anyio>=4.0

# ── 材料科学 ────────────────────────────────────────────────
numpy>=1.26,<2.0       # pymatgen 不兼容 2.x
pymatgen>=2023.9
ase>=3.22
spglib>=2.5

# ── 工具 ────────────────────────────────────────────────────
Pillow>=10.2
openpyxl>=3.1
python-dotenv>=1.0

# ── 测试 ────────────────────────────────────────────────────
pytest>=8.0
