# SC-Wiki Future UI Design System

## Design Principle

SC-Wiki 的未来规划示例页面采用高端、极简、带编辑排版杂志感的科技平台风格。页面通过黑白灰单色、超大标题、大量留白、精细网格线、克制动效和清晰的信息层级建立专业感。

本文件是未来前端示例页面的唯一全局视觉规范来源。颜色、按钮、圆角、字体、动效、状态表达和响应式规则都以本文件为准。历史需求中的蓝绿主题、灰蓝企业后台感、紫色科技炫光、大面积渐变、8px 卡片圆角等规则全部作废，并不得在新的 future-plan HTML 示例中继续出现。

## Color Tokens

```css
:root {
  --background: oklch(0.985 0.002 90);
  --foreground: oklch(0.12 0.01 60);
  --muted: oklch(0.45 0.02 60);
  --border: oklch(0.88 0.01 90);
  --surface: #ffffff;
  --surface-subtle: oklch(0.96 0.003 90);
  --surface-strong: oklch(0.92 0.004 90);
  --inverse: #ffffff;
  --radius: 0.25rem;
}
```

- 全站严格使用黑白灰单色系。
- 主按钮、选中态、关键强调均使用 `--foreground`，不得使用蓝、绿、紫、橙等彩色主题。
- 状态不依赖红绿黄蓝表达，优先使用文字、边框、透明度、斜纹和位置。
- 图表示例允许使用不同线型、点形、透明度和灰阶，不使用彩色图例作为主视觉。

## Typography

- 展示标题：`Instrument Serif`, Georgia, serif。
- 正文/UI：`Instrument Sans`, Inter, system-ui, sans-serif。
- 标签/数据/代码：`JetBrains Mono`, ui-monospace, monospace。
- Hero 标题：`clamp(3rem, 12vw, 10rem)`，行高 `0.9`，字距微收紧。
- 页面标题：48-72px。
- 模块标题：24-32px。
- 正文：15-16px。
- 辅助说明：12-14px。
- 标签、编号、状态、数据后缀使用等宽字体。

## Layout

- 内容容器最大宽度为 `1400px`。
- 页面横向内边距：移动端 `24px`，桌面端 `48px`。
- 以 8px 为基础单位，但实际页面节奏偏宽松，常用间距为 `32 / 48 / 64 / 96 / 128px`。
- 示例页首屏采用 editorial hero：超大标题、短说明、关键指标或操作，不做营销卡片堆叠。
- 功能页面采用“顶部大标题 + 主工作区”的结构。
- 双列布局用于“列表 + 详情”“表单 + 预览”“画布 + 信息面板”等明确工作流。

## Components

### Buttons

- 主按钮：黑底白字，胶囊形，右侧箭头，hover 时箭头轻微右移。
- 次按钮：透明底、细描边、hover 浅灰底。
- 危险或谨慎操作不用彩色按钮，使用斜纹边框、确认文案和影响范围表达。

### Cards And Panels

- 卡片白底，极细边框，圆角 `0.25rem`。
- 默认不使用厚重阴影；需要层级时使用 1px 边框和轻微位移。
- 页面不得堆砌无意义卡片，卡片只用于清晰分组的工作区、详情面板、证据卡片和数据预览。

### Forms

- 输入框使用细边框、单色 focus ring。
- 上传区域使用 `.border-sketch` 斜纹虚线边框。
- 表单分组必须有明确业务标题，不使用纯装饰分隔。

### Tables

- 表格使用细网格线、等宽数字、紧凑行高。
- 选中行使用浅灰底和左侧黑线。
- 表格详情优先放入右侧面板或下方详情区，不在表格中塞满长文本。

### Charts

- 图表容器使用细边框和网格背景。
- 点、线、区域用灰阶、线型、点形、透明度区分。
- 图例使用等宽标签，避免彩色徽章。
- 图表的 hover、选中、隐藏、个人配置状态必须有文字说明。

### Tags And Status

- 状态标签使用等宽字体。
- 推荐状态文案：`READY`、`MISSING FIELD`、`PENDING REVIEW`、`NOT PUBLIC DATA`、`LOCAL ONLY`、`EXTERNAL SOURCE`。
- 状态不得依赖彩色区分。

## Visual Texture

- 页面可使用低透明度细网格线背景。
- 页面可叠加 3% 左右噪点层。
- 可使用单色半透明 3D 几何体作为背景装饰，但不能遮挡内容或成为主要信息。
- 高亮文字使用半透明黑色块，不使用彩色下划线。
- 斜纹虚线边框用于上传区、待建设能力、候选记录和谨慎操作。

## Motion

- 入场动效：轻微上移、模糊、淡入。
- hover 动效：`translateY(-4px)`，不超过 240ms。
- 按钮箭头：水平位移。
- 跑马灯只用于低干扰数据流、年份、来源名或指标，不承载核心操作。
- 所有动效尊重 `prefers-reduced-motion`。
- 禁止炫光、发光、彩色渐变动画。

## Responsive

- 移动端优先。
- 大屏可双列或三列，移动端全部收敛为单列。
- 表格、图表、代码块允许横向滚动。
- 右侧详情面板在移动端变为下方详情区。
- 示例页导航保持静态表达，不实现复杂路由切换。

## Prohibited

- 禁止紫色科技炫光。
- 禁止大面积彩色渐变。
- 禁止灰蓝企业后台感。
- 禁止蓝绿旧主题变量，例如 `--blue`、`--green`。
- 禁止每个页面单独定义独立视觉语言。
- 禁止为了装饰堆叠无意义卡片。
- 禁止在模块 HTML 中覆盖本文件的全局颜色、按钮、字体、圆角和动效原则。
