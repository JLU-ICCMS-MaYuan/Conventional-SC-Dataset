# 需求质量检查表：统一「研究驱动力」字段

**目的**：审查跨层字段改名的需求完整性。本 Feature 最大风险是「某一层漏改导致读写
不对称」与「误改同名但无关的字段」，需求文本必须把受影响范围与排除范围都写死。

**创建日期**：2026-08-31

**Feature**：[spec.md](../spec.md)

## 完整性

- [ ] CHK001 受影响位置是否穷举到具体文件与行号，而非泛称「前后端」？
  plan.md 列出四层共 20 处具体位置，均经 grep 核实。
- [ ] CHK002 是否明确列出**不得改动**的同名字段？
  是。spec 范围外事项与 research.md 决策7 均标明 `backend/rag/inspiration/` 下的
  `rationale` 是灵感探索模块的「思考模式选择原因」，全局替换会破坏该功能。
- [ ] CHK003 存量数据处置是否有明确结论与依据？
  是。实测仅 1 行有内容，属旧语义，确认不迁移（FR 边界 + research.md 决策2）。
- [ ] CHK004 删列的不可逆性是否被记录？
  是。plan.md 风险表列明，且 `downgrade()` 只能重建空列、无法恢复数据。
- [ ] CHK005 AI 提示词的内容要求是否可验证？
  是。FR-004 给出四要素（内容、来源、字数、格式），SC-004 可核对字数与分条标记。

## 清晰度与可度量性

- [ ] CHK006 「统一命名」是否有客观判据？
  是。SC-002 要求三处界面显示指定标签且无旧标签；SC-003 要求全仓库标识符清除。
- [ ] CHK007 字数与格式要求是否具体？
  是。≤500 字、按 `1. 2. 3.` 分条，均可机械核对。
- [ ] CHK008 「信息不足」时的行为是否规定？
  是。US3 场景2 与 FR-004 均要求留空或如实反映，不编造。

## 一致性与覆盖

- [ ] CHK009 与 Issue #59 的关系是否说明清楚？
  是。spec 背景专节说明 #59 的结论在当时正确，本 Feature 是语义变更而非推翻；
  否则日后对照 Overview 会误以为改回去了。
- [ ] CHK010 与 Issue #65 的 `pre-wrap` 约定是否冲突？
  不冲突。FR-009 明确保留 `pre-wrap`；已核实 `multiline-text-preserved.test.tsx`
  只断言 `summary` 与 `key_finding`，不涉及本字段。
- [ ] CHK011 与 Issue #67 的边界是否清晰？
  是。本 Feature 只用 PDF 引言，联网增强属 #67；#67 依赖本 Feature 的提示词先落地。
- [ ] CHK012 实施顺序是否考虑了代码与库结构的匹配？
  是。research.md 决策6 说明必须先改代码后执行迁移，tasks 阶段 5 明确置于最后。
- [ ] CHK013 审核快照的改动是否说明了对既有追溯能力的影响？
  是。research.md 决策3 说明移除的是恒空键，追溯主体（材料状态、scope 证据）保留，
  且历史记录不回溯修改。

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- CHK002 是本 Feature 最需警惕的一项：`rationale` 在灵感探索模块有 10 处同名但无关的
  使用，一次不加区分的全局替换就会破坏该功能。
