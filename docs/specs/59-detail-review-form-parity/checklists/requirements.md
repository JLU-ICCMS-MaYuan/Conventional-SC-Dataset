# 需求质量检查表：只读详情页与校对表单一致

**目的**：核验「两个页面必须一致」这种天然模糊的诉求是否被翻译成了可判定的需求，避免实施阶段凭主观感觉决定「算不算一致」。

**创建日期**：2026-08-27

**Feature**：[spec.md](../spec.md)

## 完整性

- [x] CHK001 四个根因是否都有对应功能需求：根因 1（多出 min/max）→ FR-004、FR-005；根因 2（缺分类字段）→ FR-001、FR-002、FR-003；根因 3（结构预览来源）→ FR-009；根因 4（研究方法形态与研究理由错配）→ FR-006、FR-007、FR-008。
- [x] CHK002 死代码清理是否有需求承载：FR-010、SC-005；并在澄清记录说明为何由本 Feature 而非 #57 完成。
- [x] CHK003 回归保护是否成文：FR-011 明确列出权限提示分流、返回入口与 #56 的地址驱动行为。
- [x] CHK004 每个用户故事是否可独立验收：三个故事各有「独立验收」段，US1/US2 由详情页字段与标签证明，US3 由结构预览证明。
- [x] CHK005 是否存在无需求支撑的成功标准：SC-001~SC-006 均可回溯至 FR。
- [x] CHK006 是否存在无成功标准的功能需求：FR-001~FR-003 → SC-001/SC-002；FR-004、FR-005 → SC-001/SC-003；FR-006、FR-007 → SC-003；FR-008 → SC-004；FR-009、FR-010 → SC-005；FR-011 → SC-006。
- [x] CHK007 空态与异常是否覆盖：边界场景涵盖无材料状态、NULL 可选字段、多条计算上下文、原始值与解析值冲突、只读约束、研究方法为空。

## 清晰度与可度量性

- [x] CHK008 「两个页面保持一致」是否被翻译为可判定标准：SC-001 表述为「与校对页材料状态卡片逐项一致，无缺项、无多余项」，且 spec 假设中列出了该基准的具体字段清单，可逐项比对而非凭感觉。
- [x] CHK009 「不暴露 JSON 原文」是否可机械判定：SC-004 定义为「页面文本不含 `["` 或 `"]` 片段」，可断言。
- [x] CHK010 「不出现多余字段」是否可判定：SC-003 点名「最小值」「最大值」与标为「研究理由」的分类理由文本，均为可定位的具体对象。
- [x] CHK011 验收数值是否为实测值：SC-002 的 `thermodynamic stability`/200/`meV/atom`、`above 200 GPa`、`Fm-3m`/225、274 K、2.56、0.1 全部取自 paper 4 实测，且已在 #57 验证可从后端读出。
- [x] CHK012 是否避免技术方案侵入 spec：FR 只描述用户可见结果，未指定组件拆分方式、状态管理或具体渲染组件。
- [x] CHK013 单臂区间语义是否明确：FR-002 与验收场景 3 明确「不得补造缺失的一侧」，与 #54、#57 已确立语义一致。

## 一致性与覆盖

- [x] CHK014 与 #57 的边界是否清晰：范围外事项声明后端读取契约归 #57，假设段说明本 Feature 依赖其已完成的嵌套响应，不再改后端。
- [x] CHK015 与 #56 的边界是否清晰：范围外声明路由可达性归 #56，FR-011 同时要求不回归其地址驱动行为。
- [x] CHK016 是否纠正了原始需求中的错误前提：spec 假设明确记录「研究方法不存在中译英丢失，库内两端本就是英文」，与用户最初描述不同，并说明真实原因是展示形态差异。
- [x] CHK017 已知阻碍是否如实记录而非隐藏：`structure_models` 为 0 行、只能验空态，已写入假设与 US3 优先级理由。
- [x] CHK018 未采纳的方案是否留下依据：澄清记录说明 readOnly 复用路线被否的具体理由（需适配层、数组→单数会丢数据），避免后续重复讨论。
- [x] CHK019 是否避免把写入侧问题混入本 Feature：`rationale` 与 `classification_reason` 的字段分离、历史数据值冲突均列入范围外，本 Feature 只如实呈现。
- [x] CHK020 需求之间是否矛盾：FR-005（不含校对页不存在的字段）与 FR-003（展示 Tc 与计算上下文）不冲突——校对页 Tc 区确实含 λ/ωlog/μ*，已在假设段列明基准字段清单。
- [x] CHK021 Bug 分类是否有依据：spec 背景段说明为原设计缺陷，理由是两页面各自实现、从无共同字段集基准。

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- 首轮 21 项全部通过。路线选择与死代码归属已记入澄清记录，实施阶段不得再自行改变。
