# 调研记录：#28

- Nginx 把 `/api/` 交给 Go，Go 的 NoRoute 才代理 Python，因此论文查询必须由 Go 统一承载。
- 当前公开序列化可能返回审核意见、上传者、审核人和来源路径，不能只靠前端隐藏。
- `my-uploads/{id}` 只允许本人访问，重复论文属于他人时会产生假 404。
- 现有 `review_comment` 可继续作为面向上传者的温和“审核意见”，内部沟通需独立字段。

**决定**：策略和 DTO 在服务端集中处理，重复动作由后端按权限计算。
