# 需求质量检查表：管理端审核编辑页独立化并功能对齐提交页

**目的**：检查 `spec.md` 的需求文本是否完整、清晰、可度量、内部一致，并覆盖边界与降级行为。本表不测试代码或实现行为。

**创建日期**：2026-09-02

**Feature**：[spec.md](../spec.md)

## 完整性

- [x] CHK001 是否定义了独立页面的路由与保护？[FR-001、FR-002：`/admin/papers/:id/edit` + RoleRoute]
- [x] CHK002 是否定义了页面承载的能力范围？[FR-003：论文级字段、科学数据、审核区、升版警告]
- [x] CHK003 是否定义了结构表示的生成方式与来源？[FR-004、R1：落库 CIF 按需生成，`read_atoms`+`export_representations`]
- [x] CHK004 是否定义了晶胞/格式切换与预览？[FR-005、US2 场景 1]
- [x] CHK005 是否定义了下载？[FR-006、US2 场景 2]
- [x] CHK006 是否定义了表示生成失败的降级？[FR-007、边界场景]
- [x] CHK007 是否定义了保存行为？[FR-008、FR-009：两段保存、升版警告、退回提示]
- [x] CHK008 是否覆盖上传页不回退？[FR-010、SC-005]

## 清晰度与可度量性

- [x] CHK009 成功标准是否可验证？[SC-001 弹窗移除、SC-002 表示切换、SC-003 下载、SC-004 保存一致]
- [x] CHK010 「独立页面」是否明确为 SPA 路由？[澄清记录：非强制新标签，页面级视图]
- [x] CHK011 结构表示按需生成的可验证方式？[SC-002 切换预览 + 端点测试]

## 一致性与覆盖

- [x] CHK012 表示不落库的边界是否明确？[R1、范围外事项：不改 `structure_models` 与提交契约]
- [x] CHK013 弹窗移除是否明确？[FR-001、范围外事项]
- [x] CHK014 是否覆盖直接访问路由（非列表进入）？[边界场景]
- [x] CHK015 是否覆盖结构无记录的空态？[边界场景]

## 已知取舍

- [x] CHK016 是否说明按需生成而非落库的理由？[R1 备选方案与理由]
- [x] CHK017 是否说明 SPA 路由而非强制新标签的理由？[R2、澄清记录]

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- 前端新增 1 个页面组件（`AdminPaperEditPage.tsx`）+ 1 个路由；后端新增 1 个表示生成端点；`AdminPage.tsx` 移除编辑弹窗。
