# 需求质量检查表：未分配结构候选的分配与确认交互

**目的**：检查 `spec.md` 的需求文本是否完整、清晰、可度量、内部一致，并覆盖边界与降级行为。本表不测试代码或实现行为。

**创建日期**：2026-09-02

**Feature**：[spec.md](../spec.md)

## 完整性

- [x] CHK001 是否明确了未分配候选的定义与来源？[spec：`material_state_ref` 为 `unassigned:<file_id>`，由任务解析生成；关键实体]
- [x] CHK002 是否定义了候选的展示位置与形态？[FR-001、US1 场景 1：校对页「未分配结构候选」区，文件名与校验状态]
- [x] CHK003 是否定义了分配动作的完整语义？[FR-002：选择材料状态 → `material_states[N]` + confirmed]
- [x] CHK004 是否定义了无材料状态时的降级？[FR-003：禁止分配并提示]
- [x] CHK005 是否定义了校验失败候选的处理？[FR-004：不得确认，展示原因]
- [x] CHK006 是否定义了排除能力？[FR-005：`confirmation=excluded`]
- [x] CHK007 是否定义了分配后进入对应卡片面板？[FR-006、US1 场景 2]
- [x] CHK008 是否定义了提交落库契约？[FR-007：沿用 `_confirmed_candidates_by_state`，后端零改动]
- [x] CHK009 是否覆盖管理端共用？[FR-008、US2]
- [x] CHK010 是否覆盖只读模式？[FR-009、边界场景]
- [x] CHK011 是否覆盖存量数据恢复？[FR-010、US3]

## 清晰度与可度量性

- [x] CHK012 成功标准是否可验证？[SC-001 以 `structure_models` 出现记录度量；SC-002/SC-004 以详情页 3D 可见度量]
- [x] CHK013 「分配确认」是否量化为具体字段变化？[FR-002：`material_state_ref`/`confirmation`/`status` 三个字段]
- [x] CHK014 排除是否与「不参与提交」可验证对应？[FR-005 + `_confirmed_candidates_by_state` 只认 confirmed]
- [x] CHK015 数据恢复是否可核验？[SC-004：`papers.id=9` 详情页 3D 可见]

## 一致性与覆盖

- [x] CHK016 分配不可逆回未分配的决定是否记录？[澄清记录 2026-09-02、research.md R2]
- [x] CHK017 后端零改动的边界是否明确？[FR-007、范围外事项]
- [x] CHK018 与 #49/#76 的关系是否记录？[假设与依赖]
- [x] CHK019 是否覆盖了材料状态删除后候选指向失效下标的场景？[边界场景：沿用现有语义忽略]
- [x] CHK020 是否覆盖多候选、混合 PDF 提取候选？[边界场景]
- [x] CHK021 是否覆盖「分配后撤销」交互？[澄清记录：回到 unreviewed 而非 unassigned]

## 已知取舍

- [x] CHK022 是否说明为何不做后端自动分配？[research.md R1 备选方案与理由]
- [x] CHK023 是否说明为何存量修复不留脚本？[research.md R3、范围外事项、#74 先例]

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- 本 Feature 前端改动面仅 `MaterialStatesEditor.tsx`（+ i18n 字典），后端零改动；存量数据修复为一次性操作。
