# 需求质量检查表：顶栏 AI 供应商切换

**目的**：核验 spec.md 的需求文本本身是否完整、可度量、内部一致，重点覆盖凭据安全与
向后兼容两个高风险领域。

**创建日期**：2026-09-01

**Feature**：[spec.md](../spec.md)

## 完整性

- [x] CHK001 顶栏入口位置有明确定义（FR-001：头像左侧，预留中英文切换器左侧）
- [x] CHK002 预置供应商清单已枚举，且明确不含服务端密钥（FR-002、FR-004）
- [x] CHK003 「覆盖所有 AI 功能」已展开为具体调用点清单，非笼统表述（FR-012 列出 7 类调用）
- [x] CHK004 凭据存储位置、传递方式、生命周期均有需求条目（FR-007、FR-010、FR-020）
- [x] CHK005 未配置状态的行为有明确需求（FR-013：回退服务端默认，行为不变）
- [x] CHK006 后台异步任务（RQ worker）的凭据传递单独立项，未与请求路径混谈（FR-020）
- [x] CHK007 清除配置的路径有需求覆盖（FR-022）
- [x] CHK008 明确排除的范围有列举与原因（范围外事项 7 项均附理由）
- [x] CHK009 阻塞依赖已声明（假设与依赖：中英文切换 Issue 先落地）
- [x] CHK010 匿名用户场景已覆盖（边界场景：`/api/rag/chat` 不要求登录，校验一致生效）

## 清晰度与可度量性

- [x] CHK011 「密钥不泄露」已量化为可检索的验收（SC-003：四处检索命中 0 次）
- [x] CHK012 「全部 AI 功能生效」已量化（SC-002：四条链路无一落回默认）
- [x] CHK013 URL 拒绝范围已枚举具体网段，非「内网地址」模糊表述（FR-016、SC-004）
- [x] CHK014 连接测试的失败分类已量化为四类且各有提示（FR-023、SC-006）
- [x] CHK015 代码收敛程度已量化（SC-008：直读 `deepseek_api_key` 的调用点为 0 处）
- [x] CHK016 凭据清理已量化（SC-007：终态后凭据条目为 0）
- [x] CHK017 顶栏可见范围已量化（SC-001：7 个主导航页面）
- [x] CHK018 连接测试超时阈值已给出具体秒数（US4 场景 5：15 秒）
- [x] CHK019 掩码回显格式已给出具体形态（US5 场景 4：`sk-****abcd`）

## 一致性与覆盖

- [x] CHK020 与父 Epic #21 的范围冲突已解决——#21 原「不支持用户在前端保存个人 API Key」
      条目已撤销并记录改动原因，两个 Issue 不再存在相反约束
- [x] CHK021 FR-004（不预置服务端密钥）与 FR-013（未配置时回退服务端默认）不矛盾：回退
      指向现有 `settings.completion_*`，不是为新增预置供应商配密钥
- [x] CHK022 FR-015 的 https 强制与 US3 场景 5 的 localhost 放行已显式标为例外，非冲突
- [x] CHK023 FR-019（不静默回退）与 FR-013（未配置时回退）边界清晰：前者指凭据已配置但
      调用失败，后者指凭据未配置
- [x] CHK024 FR-005（模型名自由输入）与 FR-003（预填默认模型名）一致：预填仅为 placeholder
- [x] CHK025 异常场景已覆盖：凭据失效、服务端默认也未配置、凭据格式不符、多标签页配置
      不一致、请求头体积
- [x] CHK026 每个 FR 在 tasks.md 需求覆盖表中均有对应任务
- [x] CHK027 每个 SC 在 quickstart.md 中均有对应验收场景
- [x] CHK028 Embedding 不受影响的约束在 spec、plan、tasks 三处一致（FR-014、质量门、T022）

## 待实施期澄清的遗留项

以下不是需求缺口，而是需求已明确、但取值须经实测确认的项，已在 research.md 与 tasks.md
立项，不阻塞 Spec 定稿：

- [ ] CHK029 6 家供应商的实际 Base URL 与默认模型名（T002）
- [ ] CHK030 Claude OpenAI 兼容层对 `json_object` response_format 的支持（T003）
- [ ] CHK031 nginx 自定义头透传与日志脱敏的实际配置状态（T004）

## 已知接受的残余风险

- DNS rebinding：URL 校验在域名解析后按 IP 判定，解析与实际连接之间存在时间窗。彻底防御
  需 socket 连接层校验。已在 research.md D-004 记录为明确取舍，非遗漏。

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- CHK001–CHK028 已在 Spec 定稿时核验通过；CHK029–CHK031 待实施期取得实测结论后勾选。
