# 需求质量检查表：论文详情数据一致性（后端读取契约补全）

**目的**：核验本 Feature 的需求文本在完整性、可度量性与一致性上是否可直接支撑设计与实施，避免把「让详情页好看一些」这类不可验证的诉求带入实现。

**创建日期**：2026-08-27

**Feature**：[spec.md](../spec.md)

## 完整性

- [x] CHK001 三个根因是否都有对应功能需求：根因 1（不查 Tc/计算上下文）→ FR-001、FR-002；根因 2（恒零值字段）→ FR-005、FR-006；根因 3（材料状态字段裁剪）→ FR-003、FR-004。
- [x] CHK002 澄清后扩大的范围是否有需求承载：记录搜索 → FR-007、FR-008；管理员物性写入 → FR-009。
- [x] CHK003 「无数据」与「不读取数据」两种形态是否都有明确预期行为：边界与异常场景第 1、2 条分别规定空集合与 NULL 语义。
- [x] CHK004 回归保护是否成文：FR-010 明确列出权限判定、已有字段取值、图表分组编辑与分享页布局。
- [x] CHK005 每个用户故事是否有独立验收方式：三个故事均有「独立验收」段，可分别验证而不依赖其余故事。
- [x] CHK006 是否存在无需求支撑的成功标准：SC-001~SC-006 逐条可回溯至 FR，无悬空标准。
- [x] CHK007 是否存在无成功标准的功能需求：FR-001~FR-005 → SC-001~SC-003；FR-006 → SC-003；FR-007、FR-008 → SC-004；FR-009 → SC-005；FR-010 → SC-006。

## 清晰度与可度量性

- [x] CHK008 验收数值是否为实测值而非估计：274 K、2.56、0.1、250/200 GPa、`above 200 GPa`、`Fm-3m`/225、`thermodynamic stability`/200/`meV/atom` 均取自论文 4 的库内实测。
- [x] CHK009 「恒零值字段」是否有可判定的定义：FR-006 定义为「其值与数据库内容无关的字段」，可通过「改库内容后该字段是否变化」判定，不依赖主观感受。
- [x] CHK010 「与提交内容统一」是否被翻译为可验证陈述：SC-003 表述为「与库中材料状态、Tc 结果、计算上下文、物性记录逐项一致」，而非「看起来一致」。
- [x] CHK011 是否避免了技术方案侵入 spec：FR 描述能力与可见结果，未指定 Preload 写法、序列化函数名或字段命名方案。
- [x] CHK012 单臂区间语义是否明确到不会被实现误解：边界场景明确「缺失一侧保持 null，不补造边界」，并标注沿用 #54 已确认语义。

## 一致性与覆盖

- [x] CHK013 范围外事项是否与 #59 无重叠也无遗漏：详情页排版、结构预览改源、研究方法与分类理由语义、`PaperEditView` 去留全部划归 #59，且 spec 说明了 #59 对本 Feature 的依赖方向。
- [x] CHK014 与 #56 的边界是否清晰：范围外事项声明路由可达性归 #56；FR-010 同时要求不回归 #56 已确立的逐篇后端鉴权。
- [x] CHK015 与 #54 是否一致：压强单臂区间的入库语义未被本 Feature 改变，仅补充读取与呈现。
- [x] CHK016 已知阻碍是否在假设中如实记录而非隐藏：无已批准论文（影响 SC-004）、无结构模型记录（结构只能验空态）均已写入假设与依赖。
- [x] CHK017 历史数据不一致是否明确不在本次修复范围且不影响验收：物性原文值 `0` 与解析值 200 的冲突已列入范围外，边界场景规定两者原样返回。
- [x] CHK018 FR-009 是否避免了「悄悄改变数据所有权」：需求要求写入真实列或明确拒绝，不允许保留静默丢弃的中间态。
- [x] CHK019 Bug 分类是否有依据：spec 背景段说明分类为原设计缺陷，并给出理由（写入契约已迁移而读取契约未迁移，靠兼容字段维持编译）。
- [x] CHK020 是否存在相互矛盾的需求：FR-006（移除无法映射字段）与 FR-009（管理员写入真实列）方向一致；FR-010 的回归保护不与任何字段移除需求冲突，因受影响消费方已在用户故事 3 中一并纳入。

## 备注

- 本检查表检查需求文本质量，不测试代码或实现行为。
- 首轮 20 项全部通过；扩大范围的决策已记入 spec 澄清记录，实施阶段不得再次自行扩张。
